:root{
  --bg: #f6f7f2;
  --ink: #0b3d2e;
  --ink-2:#163f2f;
  --accent:#1c6b43;
  --accent-2:#0d5232;
  --pill:#144b33;
  --pill-hover:#0f3c29;
  --shadow: 0 10px 24px rgba(0,0,0,.18);
}

*{ box-sizing: border-box; }
html,body{ height:100%; }
body{
  margin:0;
  font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  color: var(--ink);
  background: var(--bg);
  line-height:1.6;
}

/* Container utilitário */
.container{
  width: min(1120px, 92%);
  margin-inline: auto;
}

/* Header */
.site-header{
  position: sticky;
  top:0;
  z-index: 50;
  backdrop-filter: saturate(120%) blur(8px);
  background: rgba(246,247,242,.7);
  border-bottom: 1px solid rgba(0,0,0,.06);
}
.header-inner{
  display:flex; align-items:center; justify-content:space-between;
  padding: .8rem 0;
}
.brand{
  font-weight:800; text-decoration:none; color:var(--ink);
  letter-spacing:.2px;
}
.nav{
  display:flex; gap:.4rem; flex-wrap:wrap;
}
.nav a.nav-btn{
  text-decoration:none;
  padding:.55rem .9rem;
  border-radius:999px;
  font-weight:600;
  background: transparent;
  border:1px solid rgba(22,63,47,.25);
  transition: transform .18s ease, box-shadow .18s ease, background .18s ease, color .18s ease;
  color:var(--ink-2);
}
.nav a.nav-btn:hover,
.nav a.nav-btn:focus{
  background:var(--accent);
  color:#fff;
  transform: translateY(-2px);
  box-shadow: var(--shadow);
}

/* Hamburger (mobile) */
.hamburger{
  display:none;
  border:0; background:transparent; font-size:1.4rem; cursor:pointer;
}
@media (max-width: 860px){
  .nav{ display:none; }
  .nav.open{ display:flex; flex-direction:column; gap:.6rem; position:absolute; right:4%; top:64px; background:#ffffffea; padding:1rem; border-radius:16px; box-shadow: var(--shadow);}
  .hamburger{ display:block; }
}

/* Hero com imagem de fundo */
.hero{
  position:relative;
  min-height: 78vh;
  display:grid;
  place-items:center;
  text-align:center;
  color:#0c2b1f;
  /* >>> Ajuste o caminho da imagem aqui conforme seu repositório <<< */
  background-image: url("img/ChatGPT\ Image\ 6\ de\ set.\ de\ 2025\,\ 21_03_54.png");
  background-size: cover;
  background-position: center;
  background-repeat:no-repeat;
}
.hero .overlay{
  position:absolute; inset:0;
  background: linear-gradient(180deg, rgba(246,247,242,.78), rgba(246,247,242,.92));
}
.hero-content{
  position:relative; z-index:1;
  padding: 3.2rem 0 2.4rem;
}
.hero h1{
  margin:0 0 .2rem 0;
  font-size: clamp(2.0rem, 4.4vw, 3.2rem);
  line-height:1.12;
  text-transform: uppercase;
  letter-spacing: .8px;
}
.subtitle{
  margin:.2rem 0 1.6rem;
  font-weight:600; opacity:.9;
}
.deck{
  margin: 0 auto .6rem;
  font-size: clamp(1.4rem, 3.2vw, 2.2rem);
  color: var(--accent-2);
}
.lead{
  max-width: 70ch;
  margin: 0 auto 1.6rem;
  font-size: clamp(1rem, 1.6vw, 1.125rem);
}

/* Botões em “pílula” com hover efeito */
.cta-row{
  display:flex; gap:.6rem; flex-wrap:wrap; justify-content:center; margin-top: .8rem;
}
.pill{
  display:inline-block;
  text-decoration:none;
  padding:.8rem 1.1rem;
  border-radius:999px;
  background: var(--pill);
  color:#fff;
  font-weight:600;
  box-shadow: 0 6px 16px rgba(0,0,0,.12);
  transition: transform .18s ease, box-shadow .18s ease, filter .18s ease;
}
.pill:hover, .pill:focus{
  transform: translateY(-3px) scale(1.02);
  box-shadow: 0 12px 28px rgba(0,0,0,.18);
  filter: brightness(1.05);
}
.pill:active{ transform: translateY(-1px) scale(.99); }